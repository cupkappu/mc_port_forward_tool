# Fabric 1.20.1 E2E Result

Status: automated build/test passed; real GUI client E2E not yet run.

Automated evidence captured at `2026-06-19 16:38:02 HKT`:

- `scripts/test_matrix.sh`
- `test 1.20.1`: `BUILD SUCCESSFUL`
- `build 1.20.1`: `BUILD SUCCESSFUL`
- Jar produced: `build/libs/mc-transport-dialer-1.20.1-0.1.0.jar`
- Expanded `fabric.mod.json` depends on Minecraft `~1.20.1`, Java `>=17`,
  Fabric Loader `>=0.19.3`, Fabric API `>=0.92.9`.

Required evidence:

- Build command and output timestamp.
- Server start command and log lines.
- Client join log lines.
- Single `tcp_probe.py` output.
- Concurrent `tcp_probe.py` output.

Server-side preparation evidence captured at `2026-06-19 16:45:16 HKT`:

- `scripts/e2e/prepare_fabric_server.sh 1.20.1 run/e2e-server-1.20.1 ...`
  completed and wrote server/client configs.
- Initial server launch with default `java` failed because the shell resolved
  Java 8. Re-run used `/opt/homebrew/opt/openjdk@17/bin/java`.
- Java 17 launch reached Fabric Loader mod discovery, then failed because the
  jar's `fabric.mod.json` used scalar entrypoint values. Source manifest has
  been changed to array entrypoints; jar rebuild and server relaunch are still
  required.
